<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HolaMundo.Views.EjemploGrid">
    <ContentPage.Content>

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="100" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ContentView Grid.Row="0" Grid.Column="0" BackgroundColor="Red" />

            <Label Text="Donde estoy" Grid.Column="1" Grid.Row="0" />

            <Label Text="PISA" Grid.Column="0" Grid.ColumnSpan="2" HorizontalOptions="Center" Grid.Row="1" />

        </Grid>

    </ContentPage.Content>
</ContentPage>
